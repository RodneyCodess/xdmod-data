version: 2.1

executors:
  min-python:
    machine:
      image: ubuntu-2004:2024.11.1
  max-python:
    machine:
      image: ubuntu-2604:2026.05.1

jobs:
  build_xdmod_images:
    parameters:
      xdmod_version:
        type: string
    machine:
      image: ubuntu-2204:current
    steps:
      - checkout
      - run:
          name: Build XDMoD web server images
          command: bash ./tests/ci/scripts/build_xdmod_img.sh "<< parameters.xdmod_version >>"
      - persist_to_workspace:
          root: .
          paths:
            - << parameters.xdmod_version >>.tar
  run_tests:
    parameters:
      xdmod_version:
        type: string
      python_version:
        type: string
    environment:
      XDMOD_VERSION: << parameters.xdmod_version >>
      PYTHON_VERSION: << parameters.python_version >>
    executor: << parameters.python_version >>
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run:
          name: Run tests against XDMoD web server image
          command: bash ./tests/ci/scripts/run_tests.sh
  
  throw-away-job:
    machine:
      image: ubuntu-2004:2024.11.1
    steps:
      - run:
          name: what python is here
          command: |
            python3 --version || true
            python3.8 --version || true
            which -a python3 python3.8 || true

workflows:
  workflow:
    jobs:
      - throw-away-job
      - build_xdmod_images:
          matrix:
            parameters:
              xdmod_version: ["xdmod-main-dev", "xdmod-11-0-dev", "xdmod-11-0"]
      - run_tests:
          requires:
            - build_xdmod_images
          matrix:
            parameters:
              xdmod_version: ["xdmod-main-dev", "xdmod-11-0-dev", "xdmod-11-0"]
              python_version: ["min-python", "max-python"]



#   build_docker:
#     parameters:
#       python_version:
#         type: string
#       xdmod_version:
#         type: string
#       xdmod_image:
#         type: string
#     docker:
#       - image: cimg/base:current
#     steps:
#       - checkout
#         #- run:
#             #name: Spin up containers and test different Python versions against different XDMoD web server versions
#             #command: ./tests/ci/scripts/run-tests.sh
#   build_VM-machine:
#     parameters:
#       xdmod_version:
#         type: string
#     machine:
#       image: ubuntu-2204:current
#     steps:
#       - checkout
#       #- run:
#           #name: spin up containers and test different python versions against xdmod-data
#           #command : ./tests/ci/scripts/step1.sh "<< parameters.xdmod_version >>"


# workflows:
#   workflow:
#     jobs:
#       - build_docker:
#           matrix:
#             parameters:
#               python_version: ["3.8", "3.14"]
#               xdmod_version: ["xdmod-main-dev", "xdmod-11-0-dev", "xdmod-11-0"]
#               xdmod_image: ["tools-ext-01.ccr.xdmod.org/xdmod:x86_64-rockylinux8.9.20231119-v11.0.0-1.0-03"]
#       - build_VM-machine:
#         parameters:
#           xdmod_version: ["xdmod-main-dev", "xdmod-11-0-dev", "xdmod-11-0"]
